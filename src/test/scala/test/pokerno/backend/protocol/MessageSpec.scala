package test.pokerno.backend.protocol

import org.scalatest._
import org.scalatest.matchers._
import org.scalatest.matchers.ShouldMatchers._

import pokerno.backend.protocol.{ Message }

class MessageSpec extends FunSpec with ClassicMatchers {
  describe("Message") {
    it("sample") {
    }
  }
}
