package de.pokerno.backend.poker

import org.scalatest._
import org.scalatest.matchers._
import org.scalatest.matchers.ShouldMatchers._

class FastHighSpec extends FunSpec with ClassicMatchers {
  describe("FastHigh") {
    it("sample") {
    }
  }
}
